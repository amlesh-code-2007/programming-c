#include <stdio.h>

int main(int argc, char const *argv[])
{
    /* code */
    float a,b;
    a =356;
    b =12;

    printf("a + b = %f\n", a+b);
    printf("a - b = %f\n", a-b);
    printf("a * b = %f\n", a*b);
    printf("a / b = %f\n", a/b);

    return 0;
}